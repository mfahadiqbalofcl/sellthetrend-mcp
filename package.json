{
  "name": "sellthetrend-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Unofficial MCP server for Sell The Trend — drives your own logged-in account via a headless browser (no public API exists).",
  "bin": { "sellthetrend-mcp": "server.mjs" },
  "scripts": {
    "start": "node server.mjs",
    "postinstall": "echo 'Next: npx playwright install chromium'"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "dotenv": "^16.4.5",
    "playwright": "^1.49.0",
    "zod": "^3.23.8"
  }
}
